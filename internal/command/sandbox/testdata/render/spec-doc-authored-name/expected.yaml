name: Keep_This_As_Is
spec:
  cluster: prod-eks
  forks:
  - customizations:
      env:
      - name: LOG_LEVEL
        value: debug
    forkOf:
      kind: Deployment
      name: route
      namespace: hotrod
